function audio = import_correctAudioTrigger(audio, handles)

    % Now the audio trigger is unrectified sine signal (or the white noise
    % in case of distracter 
    
    % plot(audio)   
    % pause